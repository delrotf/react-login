import React from "react";

const FormikGroup = (props) => {
  const { control, ...rest} = props;
  switch(control) {
    case 'input':
    case 'textarea':
    case 
  }

  return (
    <div>
    asdf
    </div>
  )
};

export { FormikGroup };
